# Server Configuration
server.port=8080

# Spring Datasource Configuration (H2 In-Memory for initial development)
spring.datasource.url=jdbc:h2:mem:orchestratordb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# PostgreSQL Datasource Configuration (Commented out, for production)
# spring.datasource.url=jdbc:postgresql://localhost:5432/orchestrator_db
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.appcatalog.orchestrator=DEBUG
